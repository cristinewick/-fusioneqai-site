#!/bin/bash
set -u

osascript -e 'display dialog "macOS is blocking this Desktop icon from reading copied GPT text. Reliable workflow: copy the GPT JSON, open Codex, and say: render my clipboard. Or use FusionEQ Report Renderer and choose a saved .txt/.json file." buttons {"OK"} default button "OK" with icon caution'
